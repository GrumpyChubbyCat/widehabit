[package]
name = "frontend"
version = "0.1.0"
edition = "2024"

[dependencies]
leptos = { version = "0.8.15", features = ["csr"] }

[profile.release]
codegen-units = 1
lto = true
opt-level = "z"
panic = "abort"
